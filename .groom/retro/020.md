## Entry: #020 -- Capability-Aware Benches Validate (2026-04-23)

**Effort:** predicted M -> actual M+
**Scope changes:** Shipping expanded to codify the `/ship` triad loop, then fix timeout degradation and stale backlog guidance found by review before squash.
**Blockers:** `Task.async_stream` timeout defaults can exit the caller unless `on_timeout: :kill_task` is set; git-native ship docs must preserve divergent local `master` before showing reset-style commands.
**Pattern:** A green CI pass did not prove the timeout path. Keep `/ci`, `/code-review`, and `/refactor` in a repeated triad loop until each pass is green, clean, and lean.

---
